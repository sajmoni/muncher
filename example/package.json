{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "refresh": "yarn cache clean && rm -rf node_modules && rm -f yarn.lock && yarn",
    "munch": "rm -rf output && muncher --input sprites --output output/spritesheet --flip",
    "munch-multiple": "rm -rf output && ./munch.sh"
  },
  "dependencies": {
    "muncher": "file:../muncher.tgz"
  }
}
