{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "refresh": "rm -rf node_modules && yarn",
    "munch": "rm -rf output && muncher --input sprites --output output/spritesheet --flip"
  },
  "dependencies": {
    "muncher": "file:../muncher.tgz"
  }
}
